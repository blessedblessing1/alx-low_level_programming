#include "main.h"
/**
 * mul -a function that multiplies two integers.
 * @a: one of the parameters to be checked
 * @b: The second parameter
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
