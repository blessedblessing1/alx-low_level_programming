#include <stdio.h>
/**
 * main -A program that prints using thr printf function
 * Return: 0 indicates a successful execution
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
