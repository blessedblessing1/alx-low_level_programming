#include <stdio.h>
/**
 * main -Desending order
 * Return: 0 for smooth execution
 */
int main(void)
{
	char b;

	b = 'z';

	while
		(b >= 'a')
		{
			putchar(b);
			b--;
		}
	putchar('\n');
	return (0);
}
