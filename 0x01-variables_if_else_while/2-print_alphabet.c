#include <stdio.h>
/**
 * main -Program that prints in lowercase
 * Return: 0 for a successful execution
 */
int main(void)
{
	char b;

	b = 'a';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}

